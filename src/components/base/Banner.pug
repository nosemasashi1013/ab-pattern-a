mixin Banner(params={})
  - const props = Object.assign({}, params)
  .banner
    h1.banner__title(aria-describedby="banner-desc")
      = props.title
    if props.description
      p.banner__description#banner-desc
        = props.description
    if props.icon
      img.banner__icon(src="../../images/common/" + props.icon alt="")