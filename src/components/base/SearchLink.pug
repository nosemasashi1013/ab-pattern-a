mixin SearchLink(params={})
  - const props = Object.assign({}, params)
    .search-link
      a(href=props.link) 
        .search-link__text-container
          span.search-link__text
            | あなたの状況・ご用件を選んで探す
          i(class="fas fa-chevron-circle-right")